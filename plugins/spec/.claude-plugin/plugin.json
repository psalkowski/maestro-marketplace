{
  "name": "spec",
  "description": "Provider-agnostic spec workflow (storage via docs-hub): spec:plan is the single front door (brainstorm + grill + spec, with an optional lean plan doc and an execution-mode decision); spec:execute runs handed-off plans through pinned subagents. Self-contained (no superpowers dependency); reads/writes project docs with plain file tools on the .docs/vault Docs Root.",
  "version": "0.3.0",
  "author": {
    "name": "Piotr Salkowski"
  }
}
